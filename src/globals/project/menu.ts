import { createMainDialog } from "../../classes/MainDialog";

export const {
  mds,
  title: menuTitle,
  compTitle,
  outFolder,
  template,
  compBtn,
  queueBtn,
  renderBtn,
  pbar,
  status,
} = createMainDialog();

